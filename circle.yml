machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - rspec

# TODO: add deployment to docker hub when this is ready https://circleci.com/docs/docker
