machine:
  services:
    - docker

dependencies:
  pre:
    - bundle
  override:
    - docker info

test:
  override:
    - bundle exec rspec

# TODO: add deployment to docker hub when this is ready https://circleci.com/docs/docker
